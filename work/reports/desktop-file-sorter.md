# 战报 · desktop-file-sorter（示范战）

- 日期：2026-09-23
- 任务说：`写个脚本，把桌面上的文件按类型自动归类到不同文件夹`
- 模式：arena（就地扮演各角色，未启用硬模式独立子代理）
- 豁免：无

## 受理（第 0 步）

原始立论：`写个脚本，把桌面上的文件按类型自动归类到不同文件夹`

| 要素 | 初始 | 终局 |
| --- | --- | --- |
| 目标 | 缺（"按类型"不可测） | 补：dry-run/实跑 + 统计验证 |
| 约束 / 边界 | 缺（重名？隐藏文件？删除？） | 补：不删除 / 不覆盖 / 不碰子夹与隐藏 |
| 验收 | 缺 | 补：样例目录 + 一键自检 |
| 架构/产出 | 缺（几个文件？改动落点？） | 补：脚本 + 映射配置 + 测试夹具 |

AI **点化**：强制停止思考，只反向贴出填空模板（见 `arena/readmes/ai.md` 状态·点化）：

```text
【任务一句话】___
【目标（做成什么样，才算成）】___
【约束 / 红线（绝不能做什么）】___
【架构】项目结构怎么摆 / 模块怎么分 / AI 产出哪些文件 / 改动落在哪：___
【验收标准（做完拿什么证明）】___
```

人类按模板补全 v1。

## 质询与裁决

### 第 1 轮（反方，**撞攻击库 `arena/attacks/`**）

命中：A-04 无交付结构 / A-05 验收主观 / A-01 目标不可测。

| # | 漏洞 | 命中 | severity | 裁定 |
| --- | --- | --- | --- | --- |
| R1 | 验收不可一键执行：「给一份混类样例目录」在哪、谁生成？"统计表"格式未定义 → 无法勾选 | A-05 | 高 | 成立 |
| R2 | 交付只有单文件，品类→扩展名映射用户无法扩展（想加 epub→books 无从下手） | A-04 | 高 | 成立 |
| R3 | 边界没写全：全大写 `.PNG`、无扩展名文件、`.` 开头隐藏文件（.env/.gitignore）如何处理？"不碰隐藏"只写了 ~$ 开头 | A-01 | 中 | 成立 |

### 第 2 轮（v2 补版）

R1 → 验收改为**可执行自检**：附 `tests/sample_fixture/`（10 个混类文件）+ `verify.py` 一键断言。R2 → 交付拆 4 文件：`sort_files.py` + `extensions.json`（可改，也可 `--extensions-file` 换）+ 夹具 + 验证器。R3 → 扩展名统一小写比较；无扩展名与 `.` 开头隐藏一律不处理；空目录不建。

裁判：无高危有效漏洞 → **PASS**（人类胜，第 2 轮）。

## 败约

- **AI 败**。人类加码**追加进 `arena/decrees/ai.md`**：凡工具/脚本类立论，反方先攻「验收能否一键执行」与「用户扩展点（映射可配置性）」，再攻边界。
- 攻击库 co-evolve：本场两个新攻击模式 `A-07 验收不可一键执行`、`A-08 扩展点缺失` 已入库 `arena/attacks/README.md`，下一场新题先撞到它们。

## 定稿

- `work/prompts/desktop-file-sorter-v1.md`
- 本场只过门，未执行脚本（执行走 `work/tasks/`）。